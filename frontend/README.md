# Frontend Dashboard

Next.js dashboard for the Apple News Intelligence Platform.

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL=http://localhost:8000` when the FastAPI backend is not running on the default URL.
